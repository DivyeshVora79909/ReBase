{
  "name": "rebase-surreal-compiler",
  "version": "0.2.0",
  "private": true,
  "description": "SurrealQL-first Schema-as-Code compiler for ownership, DAG RBAC, RLS, and reactive views.",
  "scripts": {
    "build": "node --env-file=.env compile.js",
    "check": "node --env-file=.env compile.js --check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@faker-js/faker": "^9.9.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "json-schema-faker": "^0.5.9",
    "seedrandom": "^3.0.5",
    "simple-statistics": "^7.9.3",
    "surrealdb": "^2.0.8"
  }
}
